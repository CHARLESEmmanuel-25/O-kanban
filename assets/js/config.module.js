const config = {
    // * Faire des variables d'environnement avec vite (lire la doc et montrer à laurent comment faire)
    // ! changer le port ou l'URL si nécessaire
    base_url: 'http://localhost:3000',
};

export { config };
